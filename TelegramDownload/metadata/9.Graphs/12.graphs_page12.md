# PAGE 5

# GRAPH PATTERN GUIDE
**PAGE 5 — GRAPH DFS PROBLEM GUIDE (APPLY TEMPLATES)**
*"Same DFS. Different problems. Only the changing parts."*

---

## HOW TO USE THIS PAGE
① Find the problem.
② Identify the Base Template (from Page 4).
③ Read the CHANGED LINES.
④ Understand the AHA.
⑤ Write code in the interview.

## TEMPLATE LEGEND (From Page 4)
* **G** Graph DFS (Adj List)
* **Gd** Grid DFS
* **N** Node DFS
* **OL** Outer Loop DFS
* **Bd** Border DFS

---

## 1 VALID PATH (G)
**Base Template:** Graph DFS (Adjacency List)

**Problem**
Determine if there is a path between source and destination.

**Changed Lines (from Template)**
```java
// 1. Add destination and return boolean
if (node == destination) return true;

// 2. Change return type and propogate
for (int neighbor : graph.get(node)) {
    if (!visited[neighbor]) {
        if (dfs(neighbor, destination, graph, visited))
            return true;
    }
}
return false;
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| `dfs(source, destination, graph, visited)` | `boolean` | `boolean[]` |

💡 **Stop early when destination is found.**

**Complexity**
**Time:** O(V + E)
**Space:** O(V)

**Interview Tip**
Mention early termination optimization.

---

## 2 CONNECTED COMPONENTS (G)
**Base Template:** Outer Loop DFS (Graph DFS + Outer Loop)

**Problem**
Count number of connected components in an undirected graph.

**Changed Lines (from Template)**
```java
// 1. Outer lopp to cover all nodes
int count = 0;
for (int i = 0; i < n; i++) {
    if (!visited[i]) {
        count++;
        dfs(i, graph, visited);
    }
}

// 2. DFS remains same (void)
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| for all nodes `i` | `int (count)` | `boolean[]` |

💡 **Each unvisited node starts a new component.**

**Complexity**
**Time:** O(V + E)
**Space:** O(V)

**Interview Tip**
Explain why we need outer loop.

---

## 3 NUMBER OF ISLANDS (Gd)
**Base Template:** Outer Loop DFS (Grid DFS + Outer Loop)

**Problem**
Count number of islands (groups of 1s) in a 2D grid.

**Changed Lines (from Template)**
```java
// 1. Outer lopp over the entire grid
int count = 0;
for (int r = 0; r < m; r++) {
    for (int c = 0; c < n; c++) {
        if (grid[r][c] == '1' && !visited[r][c]) {
            count++;
            dfs(r, c, grid, visited);
        }
    }
}
// 2. DFS remains same (mark all connected land)
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| for all cells `(r, c)` | `int (count)` | `boolean[][]` |

💡 **Every new unvisited land cell = new island.**

**Complexity**
**Time:** O(m * n)
**Space:** O(m * n)

**Interview Tip**
Don't forget to mark visited.

---

## 4 FLOOD FILL (Gd)
**Base Template:** Grid DFS

**Problem**
Replace the starting pixel's color with a new color.

**Changed Lines (from Template)**
```java
// 1. Store starting color
char originalColor = image[sr][sc];

// 2. Add color check
if (image[r][c] != originalColor) return;

// 3. Change the color
image[r][c] = newColor;

// 4. Rest same (visited + 4 directions)
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| `dfs(sr, sc, image, visited, newColor, originalColor)` | `void` | `boolean[][]` |

💡 **Only change pixels with the original color.**

**Complexity**
**Time:** O(m * n)
**Space:** O(m * n)

**Interview Tip**
Boundary + color check prevent extra work.

---

## 5 PACIFIC ATLANTIC WATER FLOW (Bd)
**Base Template:** Border DFS (Multi-Source DFS)

**Problem**
Find cells that can flow water to both Pacific and Atlantic.

**Changed Lines (from Template)**
```java
// 1. Run DFS from all Pacific borders
for (int r = 0; r < m; r++) dfs(r, 0, heights, pacific);
for (int c = 0; c < n; c++) dfs(0, c, heights, pacific);

// 2. Run DFS from all Atlantic borders
for (int r = 0; r < m; r++) dfs(r, n-1, heights, atlantic);
for (int c = 0; c < n; c++) dfs(m-1, c, heights, atlantic);

// 3. Collect intersection
if (pacific[r][c] && atlantic[r][c]) add to answer;
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| Multiple border starts | `List<List<Integer>>` | `boolean[][]` (two arrays) |

💡 **Flow is reverse. Start DFS from oceans.**

**Complexity**
**Time:** O(m * n)
**Space:** O(m * n)

**Interview Tip**
Don't do DFS from every cell!

---

## 6 SURROUNDED REGIONS (Bd)
**Base Template:** Border DFS

**Problem**
Capture all regions not connected to boundary.

**Changed Lines (from Template)**
```java
// 1. Mark boundary connected 'O's
Run DFS from all borders and mark as 'safe'

// 2. Flip remaining 'O's
for (int r = 0; r < m; r++) {
    for (int c = 0; c < n; c++) {
        if (board[r][c] == 'O' && !safe[r][c])
            board[r][c] = 'X';
    }
}
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| All border cells | `void` | `boolean[][]` (safe) |

💡 **Only border-connected regions are safe.**

**Complexity**
**Time:** O(m * n)
**Space:** O(m * n)

**Interview Tip**
Two step thinking is the key.

---

## 7 CLONE GRAPH (N)
**Base Template:** Node Graph DFS

**Problem**
Clone an undirected graph.

**Changed Lines (from Template)**
```java
// Already handled in template
// Key idea:
// If node not in map -> create clone
// For each neighbor -> recurse and add edge
```

| Caller | Return | Visited |
| :--- | :--- | :--- |
| `dfs(node, map)` | `Node (cloned)` | `HashMap<Node, Node>` |

💡 **HashMap avoids infinite loop in cycles.**

**Complexity**
**Time:** O(V + E)
**Space:** O(V)

**Interview Tip**
Always check map before creating clone.

---

## QUICK REFERENCE (CHANGES OVERVIEW)

| Problem | Base Template | Main Change | Return |
| :--- | :--- | :--- | :--- |
| **1** Valid Path | **G** | Early stop + boolean | boolean |
| **2** Connected Components | **OL** | Outer loop + count | int |
| **3** Number of Islands | **OL** + **Gd** | Grid outer loop + count | int |
| **4** Flood Fill | **Gd** | Color check + change | void |
| **5** Pacific Atlantic | **Bd** | Multi-source + intersect | List<List<Integer>> |
| **6** Surrounded Regions | **Bd** | Mark safe + flip rest | void |
| **7** Clone Graph | **N** | HashMap for visited | Node |

**G** Graph DFS | **Gd** Grid DFS | **N** Node DFS | **OL** Outer Loop DFS | **Bd** Border DFS

---

## 8 INTERVIEW CHECKLIST
- [x] Identified the correct template?
- [x] Built correct representation?
- [x] Initialized visited structure correctly?
- [x] Handled all starting points?
- [x] Applied boundary / color / condition checks?
- [x] Returned correct type?
- [x] Considered disconnected components?
- [x] Can you explain time/space complexity?
- [x] Dry run on small example?
- [x] Optimized when possible?

---

## FLOWCHART (Bottom)
(?) **Problem** (Understand) ➔ **Input** (What given) ➔ **Representation** (How to model) ➔ **Helper Structure** (Visited, dist, etc.) ➔ **Pattern** (Choose DFS Template) ➔ **Tiny Logic Change** (Apply Changes) ➔ **Dry Run** (Example) ➔ **Optimize** (Time/Space)
