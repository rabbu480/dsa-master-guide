# ARRAYS & HASHING CHEAT SHEET

| PROBLEM | KEY IDEA | APPROACH / FORMULA / PATTERN | TIME COMPLEXITY | SPACE COMPLEXITY | KEY TAKEAWAYS / PATTERNS |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1. 217**<br>Contains Duplicate<br>(Easy) | Use HashSet to track seen elements. | ```java<br>Set<Integer> set = new HashSet<>();<br>for (int x : nums) {<br>    if (set.contains(x)) return true;<br>    set.add(x);<br>}<br>return false;<br>``` | O(n) | O(n) | • HashSet for fast lookup<br>• Detect duplicates |
| **2. 242**<br>Valid Anagram<br>(Easy) | Two strings are anagrams if both have same character frequency. | ```java<br>int[26] count = new int[26];<br>for (char c : s.toCharArray()) count[c-'a']++;<br>for (char c : t.toCharArray()) count[c-'a']--;<br>return all count[i] == 0;<br>``` | O(n)<br>(n = length of string) | O(1)<br>(26 letters) | • Frequency counting<br>• Array of size 26<br>• Increment / decrement |
| **3. 1**<br>Two Sum<br>(Easy) | Store number → index in a HashMap. For each num, check if target - num exists. | ```java<br>Map<Integer, Integer> map = new HashMap<>();<br>for (int i = 0; i < nums.length; i++) {<br>    int comp = target - nums[i];<br>    if (map.containsKey(comp)) return new int[]{map.get(comp), i};<br>    map.put(nums[i], i);<br>}<br>``` | O(n)<br>(n = length of array) | O(n) | • HashMap for complement<br>• Store index, not value |
| **4. 49**<br>Group Anagrams<br>(Medium) | Sort each string to create a key. Anagrams have same sorted key. | ```java<br>Map<String, List<String>> map = new HashMap<>();<br>for (String s : strs) {<br>    char[] arr = s.toCharArray();<br>    Arrays.sort(arr);<br>    String key = new String(arr);<br>    map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);<br>}<br>return new ArrayList<>(map.values());<br>``` | O(n * k log k)<br>(n = #strings,<br>k = avg length) | O(n * k) | • Sorting as key<br>• `HashMap<String, List<String>>` |
| **5. 347**<br>Top K Frequent Elements<br>(Medium) | Count frequency using HashMap, then use bucket sort (freq → list of numbers). | 1. Count freq: `Map<Integer, Integer> freq = new HashMap<>();`<br>2. Bucket sort: `List<Integer>[] bucket = new List[nums.length + 1];`<br>```java<br>for (int num : freq.keySet()) {<br>    int f = freq.get(num);<br>    if (bucket[f] == null) bucket[f] = new ArrayList<>();<br>    bucket[f].add(num);<br>}<br>```<br>3. Traverse from high freq to low, collect k elements. | O(n)<br>(n = array length) | O(n) | • Frequency Map + Bucket Sort<br>• Buckets sized by frequency<br>• Read from high to low |
| **6. 271**<br>Encode and Decode Strings<br>(Medium) | Use length-prefix encoding: length#string to uniquely separate strings. | Encode: `for (String s : strs) sb.append(s.length()).append("#").append(s);`<br>Decode: read number until '#', then next that many chars is the string. | Encode: O(n)<br>Decode: O(n)<br>(n = total length<br>of all strings) | O(n) | • Length-prefix avoids ambiguity<br>• Unique & decodable |
| **7. 238**<br>Product of Array Except Self<br>(Medium) | Use prefix & suffix products. Avoid division. | ```java<br>int n = nums.length;<br>int[] left = new int[n], right = new int[n], ans = new int[n];<br>left[0] = 1; for (int i=1; i<n; i++) left[i] = left[i-1] * nums[i-1];<br>right[n-1] = 1; for (int i=n-2; i>=0; i--) right[i] = right[i+1] * nums[i+1];<br>for (int i=0; i<n; i++) ans[i] = left[i] * right[i];<br>``` | O(n) | O(n)<br>(for output array) | • Prefix products & suffix products<br>• No division<br>• `Left[i] * Right[i]` |
| **8. 128**<br>Longest Consecutive Sequence<br>(Hard) | Use HashSet. Start counting only from the beginning of a sequence (num-1 not present). | ```java<br>Set<Integer> set = new HashSet<>();<br>for (int num : nums) set.add(num);<br>int longest = 0;<br>for (int num : set) {<br>    if (!set.contains(num - 1)) { // start of sequence<br>        int count = 1, cur = num;<br>        while (set.contains(cur + 1)) { count++; cur++; }<br>        longest = Math.max(longest, count);<br>    }<br>}<br>``` | O(n) | O(n) | • HashSet for O(1) lookup<br>• Start from sequence beginning<br>  (num-1 not present)<br>• Each number visited once |
| **9. 36**<br>Valid Sudoku<br>(Medium) | Check rows, columns and 3x3 sub-boxes using HashSet. | 1. For each row → use `Set<Character>`<br>2. For each column → use `Set<Character>`<br>3. For each 3x3 box → use `Set<Character>`<br>```java<br>3. Box loops: for (int r=0; r<9; r+=3)<br>                 for (int c=0; c<9; c+=3)<br>                     for (int i=r; i<r+3; i++)<br>                         for (int j=c; j<c+3; j++)<br>``` | O(1)<br>(Board size is fixed 9x9) | O(1)<br>(Fixed size sets) | • Use HashSet for rows, columns and 3x3 boxes<br>• Box traversal: jump by 3 |

### COMMON HASHING DATA STRUCTURES
- `HashSet<T>` → Uniqueness, Fast lookup
- `HashMap<K,V>` → Key-Value, Frequency count
- `TreeMap<K,V>` → Sorted keys
- `LinkedHashMap` → Maintains insertion order

### FREQUENCY COUNT PATTERN
```java
Map<Integer, Integer> map = new HashMap<>();
for (int num : nums) {
    map.put(num, map.getOrDefault(num, 0) + 1);
}
```

### BUCKET SORT (BY FREQUENCY)
```java
List<Integer>[] bucket = new List[nums.length + 1];
for (int num : map.keySet()) {
    int f = map.get(num);
    if (bucket[f] == null) bucket[f] = new ArrayList<>();
    bucket[f].add(num);
}
```

### 3x3 BOX TRAVERSAL (SUDOKU)
```java
for (int r = 0; r < 9; r += 3) {
    for (int c = 0; c < 9; c += 3) {
        for (int i = r; i < r + 3; i++) {
            for (int j = c; j < c + 3; j++) {
                // process board[i][j]
            }
        }
    }
}
```

### COMPLEXITY REMINDER
- **HashSet / HashMap Operations (avg):** add, remove, contains, get, put → O(1)
- **Sorting** → O(n log n)
- **Traversal** → O(n)

⭐ **REMEMBER:** Use the right tool for the right problem. Hashing improves lookup from O(n) to O(1) on average.
